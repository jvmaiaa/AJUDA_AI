package com.ajuda.ajuda_ai.api.domain.repository;

import com.ajuda.ajuda_ai.api.domain.entity.ApoiadorEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApoiadorRepository extends JpaRepository<ApoiadorEntity, Long> {
}
